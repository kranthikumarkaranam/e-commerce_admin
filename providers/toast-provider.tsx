'use client';

import { Toaster } from 'react-hot-toast';

// Component that provides the Toast functionality using the React Hot Toast library.
export const ToastProvider = () => {
	return (
		<Toaster /> // Renders the Toaster component provided by react-hot-toast.
	);
};
